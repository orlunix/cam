name: agent-doctor
version: 0.2.0
title: Agent Doctor
description: Diagnose an agent's harness setup — system prompt, memory, skills, MCP, cron — with concrete fix suggestions.
# Native extension: the view is the built-in app page (mode-agent-doctor,
# skills-style); this package carries only the remote collector (main.py).
native: agent-doctor
capabilities:
  - exec
mounts:
  - agent
